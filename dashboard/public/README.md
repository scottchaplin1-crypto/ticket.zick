Drop your custom logo image in this folder and name it `logo.png` (square works
best, e.g. 256x256, transparent background if you have one). The dashboard will
automatically pick it up everywhere the logo appears (login, server picker, sidebar).

If no `logo.png` is present, the dashboard falls back to the built-in ticket-stub
icon automatically — nothing breaks either way.
